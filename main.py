import matplotlib.pyplot as plt
import numpy as np
"""
plt.plot(startingPointx, startingPointy, 'ro')

plt.ylabel('some numbers')
plt.show()
"""
#line lengths
lineA = 5
lineB = 5

fps = 10

anchorx = 10
anchory = 10
def doublePenForm(anchorx, anchory):
    theta1 = anchorx
    theta2 =
    x1 = lineA * np.sin()
    y1 = -1 * lineA * np.cos(theta1)
    x2 = ((lineA * np.sin(theta1)) + (lineB * np.sin(theta2)))
    y2 = ((-1 *lineA * np.cos(theta1)) + (-1 * lineB * np.cos(theta2)))
